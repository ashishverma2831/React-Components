

const numbersArray = () => {
    let numbers = [];
    for (let i = 1; i <= 120; i++) {
        numbers.push(i);
    }
    return numbers;
}

const dataArray = numbersArray();
// console.log(dataArray);

export default dataArray;
